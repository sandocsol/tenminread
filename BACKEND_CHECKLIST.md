# 백엔드 확인 체크리스트

## 🔍 백엔드에서 확인해야 할 사항

### 1. 서버 실행 상태
- [ ] 백엔드 서버가 실행 중인가?
- [ ] 포트가 올바른가? (기본값: 8080)
- [ ] 서버 로그에 에러가 없는가?

### 2. API 엔드포인트 확인

#### 프론트엔드에서 호출하는 엔드포인트:
```
GET  /api/book/{bookId}                           - 책 정보 조회
GET  /api/book/{bookId}/summary/{seq}             - 책 요약 조회
GET  /api/book/{bookId}/index                     - 책 목차 조회
GET  /api/book/{bookId}/progress                  - 독서 진행 상황 조회
POST /api/book/{bookId}/favorites                 - 즐겨찾기 토글
GET  /api/books/{bookId}/summary/{summaryId}/quizzes    - 퀴즈 조회
POST /api/books/{bookId}/summary/{summaryId}/submit     - 퀴즈 제출
```

#### 확인 사항:
- [ ] 위 엔드포인트들이 모두 구현되어 있는가?
- [ ] 경로가 정확히 일치하는가? (`/book/` vs `/books/` 주의!)
- [ ] HTTP 메서드가 일치하는가?
- [ ] 경로 파라미터 이름이 일치하는가? (`bookId`, `seq`, `summaryId`)

### 3. 경로 불일치 문제

**주의**: 프론트엔드에서 다음처럼 사용하고 있습니다:
- `bookApi`: `/api/book/` (단수)
- `quizApi`: `/api/books/` (복수)

백엔드에서 경로가 다르면 수정이 필요합니다:
- 백엔드가 `/api/book/` (단수)를 사용한다면 → `quizApi.js` 수정 필요
- 백엔드가 `/api/books/` (복수)를 사용한다면 → `bookApi.js` 수정 필요
- 또는 백엔드 경로를 프론트엔드와 맞추기

### 4. 요청/응답 형식 확인

#### 책 요약 조회 API (`GET /api/book/{bookId}/summary/{seq}`)

**요청 파라미터:**
- `bookId`: string 또는 number
- `seq`: string 또는 number (요약 순서)

**예상 응답 형식:**
```json
{
  "bookId": 1,
  "seq": 1,
  "summaryText": "요약 내용...",
  "version": "v1"
}
```

**확인 사항:**
- [ ] `bookId`를 올바르게 처리하는가?
- [ ] `seq`를 올바르게 처리하는가?
- [ ] 응답 형식이 예상과 일치하는가?
- [ ] 필드 이름이 일치하는가? (`summaryText`, `bookId`, `seq`, `version`)

### 5. CORS 설정

**확인 사항:**
- [ ] CORS가 올바르게 설정되어 있는가?
- [ ] 개발 환경에서 `http://localhost:5173` (Vite 기본 포트) 허용되어 있는가?
- [ ] 필요한 HTTP 메서드(GET, POST 등)가 허용되어 있는가?
- [ ] 필요한 헤더가 허용되어 있는가?

### 6. 에러 처리

**404 에러 발생 시 확인:**
- [ ] 해당 엔드포인트가 구현되어 있는가?
- [ ] 라우팅 설정이 올바른가?
- [ ] 요청이 올바른 컨트롤러로 전달되는가?
- [ ] 파라미터가 올바르게 파싱되는가?

**500 에러 발생 시 확인:**
- [ ] 서버 로그에 에러가 있는가?
- [ ] 데이터베이스 연결이 정상인가?
- [ ] 예외 처리가 올바른가?

### 7. 데이터베이스 확인

**확인 사항:**
- [ ] 데이터베이스가 실행 중인가?
- [ ] 테스트 데이터가 존재하는가?
- [ ] `bookId=1`, `seq=1`인 데이터가 존재하는가?
- [ ] 데이터베이스 스키마가 올바른가?

## 🔧 백엔드에서 직접 테스트하는 방법

### 1. Postman 또는 curl 사용

```bash
# 책 정보 조회
curl -X GET http://localhost:8080/api/book/1

# 책 요약 조회
curl -X GET http://localhost:8080/api/book/1/summary/1

# 독서 진행 상황 조회
curl -X GET http://localhost:8080/api/book/1/progress

# 퀴즈 조회
curl -X GET http://localhost:8080/api/books/1/summary/1/quizzes
```

### 2. 백엔드 로그 확인

API 호출 시 백엔드 로그에서 다음을 확인:
- [ ] 요청이 도달했는가?
- [ ] 어떤 엔드포인트로 요청이 들어왔는가?
- [ ] 파라미터가 올바르게 전달되었는가?
- [ ] 어떤 에러가 발생했는가?

### 3. 스웨거/API 문서 확인

- [ ] API 문서와 실제 구현이 일치하는가?
- [ ] 엔드포인트 경로가 문서와 일치하는가?
- [ ] 요청/응답 형식이 문서와 일치하는가?

## 📋 프론트엔드와 백엔드 협업 시 확인 사항

### 1. 엔드포인트 경로 통일
- [ ] `/book/` (단수)와 `/books/` (복수) 중 하나로 통일
- [ ] 모든 관련 엔드포인트에 일관되게 적용

### 2. 파라미터 이름 통일
- [ ] `bookId` vs `book_id` vs `id`
- [ ] `seq` vs `summaryId` vs `summary_id`
- [ ] 프론트엔드와 백엔드에서 동일하게 사용

### 3. 응답 형식 통일
- [ ] 필드 이름 통일 (camelCase vs snake_case)
- [ ] 응답 구조 통일 (에러 응답, 성공 응답)

### 4. 환경 변수 통일
- [ ] 개발 환경 API 주소
- [ ] 프로덕션 환경 API 주소
- [ ] 프록시 설정

## 🚨 일반적인 문제와 해결 방법

### 문제 1: 404 Not Found

**가능한 원인:**
1. 엔드포인트가 구현되지 않음
2. 경로가 잘못됨 (`/book/` vs `/books/`)
3. 라우팅 설정이 잘못됨

**해결:**
- 백엔드 코드에서 엔드포인트 구현 확인
- 경로가 프론트엔드와 일치하는지 확인
- 라우팅 설정 확인

### 문제 2: CORS 에러

**가능한 원인:**
- CORS 설정이 없음
- 허용된 Origin이 잘못됨

**해결:**
- 백엔드에 CORS 설정 추가
- 개발 환경에서 `http://localhost:5173` 허용

### 문제 3: 500 Internal Server Error

**가능한 원인:**
- 서버 내부 에러
- 데이터베이스 연결 문제
- 예외 처리 누락

**해결:**
- 백엔드 로그 확인
- 데이터베이스 연결 확인
- 예외 처리 추가

### 문제 4: 응답 형식 불일치

**가능한 원인:**
- 필드 이름이 다름
- 응답 구조가 다름

**해결:**
- API 문서 확인
- 프론트엔드와 백엔드 코드 비교
- 필요시 변환 로직 추가

